# Commands to start on workspace startup
tasks:
  - init: npm install && npm i -g tsdx && cd demo && npm install
  - name: Run tests on watch
    command: npm run test:watch
  - name: Run dev server
    command: cd demo && npm run dev
    openMode: split-right